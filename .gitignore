# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
build/
dist/

# dbt build artefacts. target/ and target-clean/ ARE committed — they are the source the
# demo fixtures are captured from, and committing them lets anyone reproduce the scan
# without installing dbt.
demo/warehouse/logs/
demo/warehouse/dbt_packages/
demo/warehouse/*.duckdb
demo/warehouse/*.duckdb.wal

# Faultline local state
.faultline-baseline.json

# Written into the workspace by CI so hashFiles() can see it.
faultline-comment.md
